<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lims.api.approve.repository.ApprovalMapper">

    <insert id="insert" parameterType="Approval" useGeneratedKeys="true" keyProperty="id">
        insert into approval
            (
             approve_request_date,
            approval_status
            )
            values(
            #{approveRequestDate},
            #{approvalStatus}
            )
    </insert>

    <select id="findById" parameterType="Long" resultType="Approval">
        select
            id,
            approve_request_date,
            approval_status
        from approval
        where id = #{id}
    </select>

    <select id="findByIdWithLock" parameterType="Long" resultType="Approval">
        select
            id,
            approve_request_date,
            approval_status
        from approval
        where id = #{id}
        for update
    </select>

    <update id="save" parameterType="Approval">
        update approval
        set
            approval_status = #{approvalStatus}
        where id = #{id}
    </update>



</mapper>